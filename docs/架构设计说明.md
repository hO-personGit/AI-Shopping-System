# 架构设计说明（V3.0）

> 本文档面向面试与二次开发，梳理系统架构、关键设计与技术选型理由。

## 1. 总体架构

```text
┌─────────────────────────────────────────────────────────────┐
│ Vue2 前端 (8080/80)                                         │
│  用户端 / 后台管理 / AI智能导购(多轮对话+SSE打字机)           │
└───────────────┬─────────────────────────────────────────────┘
                │ HTTP /api/**（Axios 统一请求）
┌───────────────▼─────────────────────────────────────────────┐
│ SpringBoot 后端 (1234)                                       │
│ Controller → Service → Mapper(MyBatis-Plus)                 │
│ Spring Security + JWT 鉴权                                   │
│ Redis 分布式锁(防超卖) / Redis 预扣库存 / Caffeine+Redis 缓存 │
│ 缓存三防(布隆过滤器/互斥锁/TTL抖动) / 订单状态机 / Swagger    │
│ RabbitMQ(下单削峰/延迟关单) / AiService 调用 AI 微服务        │
└───────┬──────────────────────┬──────────────────────────────┘
        │                      │
   MySQL(业务数据)          RabbitMQ(订单事件/延迟队列)
        │                      │
        │              FastAPI AI 微服务 (8001)
        │              ├─ 智能导购（多轮对话+混合检索+Rerank+Agent）
        │              ├─ 文案生成 / 销售分析
        │              ├─ FAISS 向量库 / 问答缓存
        │              ├─ Rerank 语义精排(lexical/api)
        │              └─ RAG 评估闭环(RAGAS 风格)
```

**三层解耦的核心收益**：AI 能力全部收敛在 Python 微服务，更换大模型、升级向量库或新增 AI 功能都不影响 Java 后端与前端，可独立扩展与部署；MQ 与订单状态机让下单链路具备高并发吞吐与最终一致性。

## 2. 关键设计

### 2.1 AI 智能导购链路（RAG + Rerank + Agent）

```text
用户提问
  → ① 会话记忆（LRU+TTL，按 sessionId 维护最近 6 轮）
  → ② 问答缓存（同 session 同问题直接命中，降低 LLM 调用成本）
  → ③ 混合检索（FAISS 向量 + BM25 关键词多路召回 → RRF 融合）
  → ④ Rerank 语义精排（lexical 无依赖 / api 接 bge-reranker，失败自动降级）
  → ⑤ Function Calling 工具决策（search_products / query_stock / get_top_selling）
  → ⑥ 大模型生成推荐（约束 Prompt 防幻觉，只基于候选数据作答）
  → ⑦ SSE 流式返回 + 结构化推荐列表
```

**评估闭环（RAGAS 风格）**：`python -m app.evaluation.run_eval` 产出 `eval_report/eval_report.md`，
指标覆盖 recall@k / precision@k / MRR / NDCG@k（检索）与忠实度 / 相关性代理指标（生成），形成「检索-生成-评估」闭环。

**面试可深挖点**：
- 为什么混合检索？纯向量检索对精确关键词（品牌名、型号）召回弱，BM25 补足精确匹配；RRF 融合对多路分数量纲不敏感。
- 为什么加 Rerank？召回重排（粗排）只保证"相关"集合，Rerank 用更精细的模型在头部做精排，提升 Top-K 精度；lexical 模式无外部依赖可离线验证链路。
- 为什么加问答缓存？LLM 调用有延迟与成本，同 session 的高频追问可复用。

### 2.2 MQ 异步下单链路（削峰 + 最终一致性）

```text
下单请求
  → ① 校验商品 & Redis 预扣库存（Lua 原子：DECRBY + 不足回滚，防超卖）
  → ② 生成业务订单号 order_no（幂等键）
  → ③ 发送 ORDER_CREATE 消息到 RabbitMQ（削峰，接口快速返回）
  → ④ 消费者幂等落库订单（状态=待支付0），同时投递延迟关单消息
  → ⑤ 支付成功 → ORDER_PAY_SUCCESS → 状态 0→1 + DB 原子扣库存（WHERE stock>=qty）
  → ⑥ 超时未支付 → 延迟队列(TTL+DLX) → ORDER_TIMEOUT_CLOSE → 状态 4 + 回补 Redis 预扣库存
  → ⑦ 定时任务兜底扫描超时订单（MQ 消息丢失时的最终一致性保障）
```

**可靠性设计**：
- 消费者**手动 ACK + 幂等处理**（orderNo 唯一键 / 状态机判断），失败进死信队列。
- 开启 **Publisher Confirm**，发送失败自动降级本地同步调用（无 MQ 环境可运行）。
- 延迟关单：`TTL + 死信交换机(DLX)` 实现，不依赖第三方延迟插件。

### 2.3 订单状态机

```
0 待支付 ──支付──▶ 1 已支付 ──发货──▶ 2 已发货 ──确认收货──▶ 3 已完成
    │                │                    │
    └──超时/取消──▶ 4 已取消    └──申请退款──▶ 5 退款中 ──同意──▶ 6 已退款
                                                       └──拒绝──▶ 7 退款失败
```

- 状态流转由 `OrderStatus.canTransition(from, to)` 统一校验，非法流转（如待支付→已完成）直接拒绝。
- 状态变更均记录 `last_status`，支持退款、取消等回滚语义。

### 2.4 缓存三防（穿透 / 击穿 / 雪崩）

| 问题 | 防护手段 | 实现 |
|---|---|---|
| 穿透 | 布隆过滤器 + 空值短 TTL 缓存 | 自实现 `ProductBloomFilter`（按 m=-n·ln(p)/ln2² 公式分配）拦截确定不存在的 key；空结果缓存 30s |
| 击穿 | 互斥锁 + 双检 | Redis 分布式锁 / JVM 本地锁串行化重建，双检避免重复打库 |
| 雪崩 | TTL 随机抖动 | 写入时 TTL 叠加 0~120s 随机值，错峰过期 |

- 两级缓存：Caffeine 一级（毫秒级）+ Redis 二级（多实例共享，可开关）。
- 商品写操作（增删改/状态）自动失效对应缓存。

### 2.5 防超卖（双层兜底）

- **下单层**：Redis Lua 原子预扣库存，并发下单快速拒绝超卖。
- **支付层**：分布式锁（SET NX EX + Lua 释放）串行化 + DB 原子 UPDATE（`WHERE stock >= quantity`）最终兜底。
- Redis 不可用：自动降级本地锁 / 同步校验，保证演示环境可用。

## 3. 目录与职责

| 模块 | 职责 | 技术 |
|---|---|---|
| `vue/` | 前端展示与交互 | Vue2 · Element UI · ECharts |
| `springboot/` | 业务后端 + AI 网关 + MQ 下单 | Spring Boot 3.4 · MyBatis-Plus · Redis · RabbitMQ |
| `ai-service/` | AI 能力服务 | FastAPI · LangChain · FAISS · Rerank |
| `perf/` | 性能压测资产 | JMeter · Python 压测脚本 |
| `deploy/` | 编排与部署 | docker-compose · K8s |
